# Archon API Authentication Configuration
# Created: 2025-10-15
#
# Add these environment variables to your .env file to enable API key authentication

# Enable/disable authentication (default: true)
# Set to "false" only for local development
AUTH_ENABLED=true

# Allowed CORS origins (comma-separated)
# For production, specify your domain
# For development, you can use "*" to allow all origins
ALLOWED_ORIGINS=https://archon.nexorithm.io,http://localhost:3737

# Bootstrap secret for creating the first API key
# Generate a secure random value with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# This is only used once to create the initial admin API key
ARCHON_BOOTSTRAP_SECRET=your-secure-random-secret-here

# Example API key (after bootstrap)
# Store your API key securely - never commit to version control!
# ARCHON_API_KEY=ak_XXXX_your-actual-api-key-here
